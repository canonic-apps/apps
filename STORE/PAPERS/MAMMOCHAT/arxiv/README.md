# arXiv Bundle

Run `scripts/arxiv_bundle.sh` to generate `mammochat-arxiv.zip`.

The bundle is assembled from `tex/` sources and patches the bibliography
backend to `bibtex` for arXiv compatibility.
